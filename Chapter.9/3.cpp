#include <bits/stdc++.h>
#define fastio cin.tie(0)->sync_with_stdio(0)
using namespace std;

int main() {
    fastio;
    cout << cout.width() << endl; // 출력 문자폭
    cout << cout.precision() << endl; // 부동 소수점의 출력 자릿수
    cout << cout.fill() << endl; // 채움 문자(공백)
}
